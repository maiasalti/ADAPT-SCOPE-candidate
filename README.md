# FinGuard

A small insurance-claims API used for internal SCOPE assessments.

## Setup

    npm install
    npm test    # existing-behavior tests
    npm run dev # start the server on :3000

## Working on a ticket

See `TICKET.md` for the current task and `CLAUDE.md` for the engineering
guardrails your changes are expected to follow.
